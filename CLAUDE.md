# CLAUDE.md

Context for Claude Code sessions in this repo.

## What this repo is

`0g` is vee's general-purpose repo for notes, experiments, and small projects. See `README.md` for the suggested folder layout. There is no build, test suite, or CI yet.

## Owner

- GitHub: `rabbitvee123`
- Call the user "vee".
- Number each reply (`#01`, `#02`, ...), continuing from the last reply in the session.

## Google Drive mirror

The folder `0/r/0g/` in vee's Google Drive mirrors this repo. Folder id: `1Edz-B3sCuiQej3pXYYQzVqGMnp2DGX2i`.

- It is a **manual copy**, not a live sync. After changing files here, copy the changed files to that Drive folder when asked.
- Upload markdown as `text/markdown` with Google Doc conversion disabled, so files stay as `.md`.
- Later, other `rabbitvee123` repos will also be mirrored under the same Drive folder, one subfolder per repo.

## Planned

- Automatic GitHub → Drive sync (a Replit app triggered on push, or a scheduled job). Not built yet.

## Conventions

- Default branch is `main`. Push small changes straight to `main` unless vee asks for a branch or pull request.
- Keep files plain markdown where possible.
- Don't add licenses, CI, or tooling unless asked.
